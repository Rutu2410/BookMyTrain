<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Trainn" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize the status bar color -->
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item> <!-- Set true for dark text/icons -->
    </style>

    <style name="Theme.Trainn" parent="Base.Theme.Trainn" />
</resources>
